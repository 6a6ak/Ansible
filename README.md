# Ansible
## Bootstrap
Making elementary need files to startup with Ansibale 
It makes directories in /homa/username/Ansible

```html
└──Ansible
    ├── inventory.ini
    ├── playbook.yml
    └── roles
        └── example_role
            └── tasks
                └── main.yml

```
