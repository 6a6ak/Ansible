## Ansible
### Bootstrap
Genesis.sh 
    - Makes elementary need files to startup with Ansible 
        It makes directories in /homa/username/Ansible

```html
└──Ansible
    ├── inventory.ini
    ├── playbook.yml
    └── roles
        └── example_role
            └── tasks
                └── main.yml

```
